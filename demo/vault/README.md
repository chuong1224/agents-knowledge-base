# Demo vault

A synthetic ~120-note vault used for the screenshots and GIFs in the main README.
Try the app on it: clone this repo into `demo/vault/.graph3d` (or copy the app files there),
then run `python ensure_graph3d.py` from that folder.
